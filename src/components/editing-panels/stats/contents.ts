import { InputsTypes } from 'types';

const fields = [
  {
    type: InputsTypes.TEXT,
    path: 'content.graphs.stats.custom_title',
    label: 'Custom title',
    props: {},
  },
];

export { fields };
