import { PieChart } from '@styled-icons/feather';

import { Blocks } from 'types';

const contents = [
  {
    icon: PieChart,
    blockType: Blocks.STATS,
    name: 'Estatísticas',
  },
];

export { contents };
