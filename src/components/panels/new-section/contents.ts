import { PieChart } from '@styled-icons/feather';

import { Sections } from 'types';

const contents = [
  {
    icon: PieChart,
    sectionType: Sections.STATS,
    name: 'Estatísticas',
  },
];

export { contents };
