const baseUrl = 'https://github-readme-stats.vercel.app/api';

const urls = {
  stats: {
    url: `${baseUrl}`,
    alt: 'Stats graph',
  },

  languages: {
    url: `${baseUrl}/top-langs`,
    alt: 'Stats language graph',
  },
};

export { urls };
