import { Inputs } from 'types';

const groups = [
  {
    id: 1,
    label: '',
    fields: [
      {
        type: Inputs.SWITCH,
        path: 'styles.clear',
        label: 'Clear float',
      },
    ],
  },
];

export { groups };
